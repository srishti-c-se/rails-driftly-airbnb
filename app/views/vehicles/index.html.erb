<%# <h1>Vehicles#index</h1>
<p>Find me in app/views/vehicles/index.html.erb</p> %>
<ul>
  <% @vehicles.each do |vehicle| %>
    <li>
      <%# <p><%= vehicle.title %></p>

      <strong><%= link_to vehicle.title, vehicles_path(vehicle) %></strong>
    </li>
      <%= vehicle.address %>
  <% end %>
</ul>
